<h2>Message Metrics</h2>

<p>Total messages sent: <%= @total_messages_sent %></p>
<p>Messages sent today: <%= @messages_sent_today %></p>

<table>
  <tr>
    <th>Receiver</th>
    <th>Body</th>
    <th>Status</th>
    <th colspan="3"></th>
  </tr>
 
  <% @messages.each do |message| %>
    <tr>
      <td><%= message.to %></td>
      <td><%= message.body %></td>
      <td><%= message.status %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Back to Articles', articles_path %>
